<div class="container">
  <h3 class="my-4 text-center marker-border-blue">登録レース情報</h3>
  <div class="row">
    <div class="col-md-5">
      <div class="center">
        <%= image_tag @race.get_race_image(350,350) %>
      </div>
    </div>
    <div class="col-md-7 mt-2">
      <p class="text-left">開催予定日：<%= @race.date.strftime('%Y/%m/%d') %></p>
      <p class="text-left">申し込み締め切り日：<%= @race.deadline.strftime('%Y/%m/%d') %></p>
      <p class="text-left">開催地：<%= @race.venue %></p>
      <p class="text-left">地域：<%= @race.district.name %></p>
      <p class="text-left">出場費(税込み)：<%= @race.cost %> 円</p>
      <p class="text-left">開催種目：<%= @race.categorie %></p>
      <p class="text-left">公式サイト：<%= link_to @race.link,@race.link %></p>
      <% if @race.is_active == true %>
        <p class="text-left">公開ステータス：公開中</p>
      <% else %>
        <p class="text-left">公開ステータス：非公開中</p>
      <% end %>
    </div>
  </div>
  <p class="text-center"><%= link_to "登録情報を変更する" ,edit_admin_race_path(@race) ,class: "btn btn-primary"%></p>
  <p class="text-center"><%=link_to "大会一覧に戻る", admin_root_path ,class: "btn btn-secondary"%></p>
</div>